<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/css/app.css">
    <title>Show</title>
</head>
<body>

   <h1><%=keyboard.name %></h1>

    <nav>
        <a class="link" href="/keyboards">Back to All Keyboards (Unsorted)</a>
    </nav><br><br>

    <div class="grid-container">
    Keyboard Size: <%=keyboard.keyboardSize %><br>
    <% if (keyboard.otherSize) { %>
        Other Size: <%= keyboard.otherSize %><br>
      <% } %>
    Connectivity: <%=keyboard.connectivity %><br>
    Switches: <%=keyboard.switches %><br>
    Switch Facing: <%=keyboard.switchFacing %><br>
    Hot-Swappable? <%=keyboard.hotswap %><br>
    <% if(keyboard.hotswap === "on"){ %> checked <% } %> 
    Knob? <%=keyboard.knob %><br>
    <% if(keyboard.knob === "on"){ %> checked <% } %> 
    Body Color(s): <%=keyboard.color %><br>
    Body Material(s): <%=keyboard.bodyMaterials %><br>
    Plate Options: <%=keyboard.plateOptions %><br>
    Backlit/RGB? <%=keyboard.rgb %><br>
    <% if(keyboard.rgb === "on"){ %> checked <% } %> 
    Price: $<%=keyboard.price %><br>
    <img src= <%= keyboard.img %> alt= <% keyboard.name %> >
    </div><br>

    <a class="link" href="/keyboards/<%=keyboard._id %>/edit">Edit</a><br>
    <form action="/keyboards/<%= keyboard.id %>?_method=DELETE" method="POST">
        <input class="delete" type="submit" value="DELETE" />
    </form>

</body>
</html>