<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/css/app.css">
    <title>Show</title>
</head>
<body>
    <h1><%=keyboard.name %></h1>
    
    <nav>
        <a class="link" href="/keyboards">Back to All Keyboards</a>
    </nav>

    Keyboard Size: <%=keyboard.keyboardSize %><br>
    <% if (keyboard.otherSize) { %>
        Other Size: <%= keyboard.otherSize %><br>
      <% } %>
    Color(s): <%=keyboard.color %><br>
    Connectivity: <%=keyboard.connectivity %><br>
    Switches: <%=keyboard.switches %><br>
    Switch Facing: <%=keyboard.switchFacing %><br>
    <!-- Hot-Swappable: <%=keyboard.hotswap %><br>
    <% if(keyboard.hotswap === true){ %> checked <% } %>  -->
    Stabilizer Type: <%=keyboard.stabilizerType %><br>
    <!-- Knob: <%=keyboard.knob %><br>
    <% if(keyboard.knob === true){ %> checked <% } %>  -->
    Body Materials: <%=keyboard.bodyMaterials %><br>
    Plate Options: <%=keyboard.plateOptions %><br>
    <!-- Backlit/RGB: <%=keyboard.rgb %><br>
    <% if(keyboard.rgb === true){ %> checked <% } %>  -->
    Price: $<%=keyboard.price %><br>
    <img src= <%= keyboard.img %> alt= <% keyboard.name %> ><br><br>
    <a href="/keyboards/<%=keyboard._id %>/edit">Edit</a> <br><br>
    <form action="/keyboards/<%= keyboard.id %>?_method=DELETE" method="POST">
        <input class="delete" type="submit" value="DELETE" />
    </form>
</body>
</html>